<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jirath.jirathblog2.dao.CommentDao">
    <insert id="addComment" parameterType="Comment">
        INSERT INTO comment (blog_id, comment_order, comment_content, comment_author_id, comment_time)
        VALUES (#{blogId},#{commentOrder},#{commentContent},#{commentAuthorId},now())
    </insert>
    <delete id="deleteByBlogId" parameterType="int">
        DELETE FROM comment WHERE blog_id=#{value}
    </delete>
</mapper>